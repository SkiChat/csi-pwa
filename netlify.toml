[build]
  command = "npm run build"
  publish = "dist"
[functions]
  directory = "../workers"
    node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
