[build]
  command = "npm run build --workspace=@polymarket-intelligence/web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
